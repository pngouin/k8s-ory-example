# Source: kratos/templates/service-admin.yaml
apiVersion: v1
kind: Service
metadata:
  name: test-kratos-admin
  namespace: default
  labels:
    app.kubernetes.io/name: kratos
    helm.sh/chart: kratos-0.5.3
    app.kubernetes.io/instance: test-kratos
    app.kubernetes.io/version: "0.5.2-alpha.1-sqlite"
    app.kubernetes.io/managed-by: Helm
spec:
  type: ClusterIP
  ports:
    - port: 4434
      targetPort: http-admin
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: kratos
    app.kubernetes.io/instance: test-kratos